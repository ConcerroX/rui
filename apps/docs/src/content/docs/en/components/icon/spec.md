---
title: Icon
docSlug: components/icon
tab: spec
locale: en
designOrder: 50
developOrder: 50
api: /en/api/icon
---

## Structure

Icon is a small primitive for rendering inline SVG content with consistent sizing and accessibility behavior.

Provide the SVG contents through the default slot. Use `size` to control the rendered dimensions, and use `label` only when the icon needs its own accessible name.
